package com.enterprise.datacache.feature.cache.model;

/** Outcome of the validation phase run against a BUILDING version before activation. */
public enum ValidationStatus {
    NOT_RUN,
    PASSED,
    FAILED
}
