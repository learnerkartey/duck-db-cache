apiVersion: v1
kind: Service
metadata:
  name: data-cache-service
  labels:
    app: data-cache-service
spec:
  selector:
    app: data-cache-service
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
