# EXAMPLE ONLY - do not commit real credentials. Create the real Secret out-of-band, e.g.:
#   oc create secret generic data-cache-dremio-credentials \
#     --from-literal=DREMIO_USERNAME=svc_datacache \
#     --from-literal=DREMIO_PASSWORD='********'
apiVersion: v1
kind: Secret
metadata:
  name: data-cache-dremio-credentials
type: Opaque
stringData:
  DREMIO_USERNAME: "REPLACE_ME"
  DREMIO_PASSWORD: "REPLACE_ME"
